apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-omnimind
  labels:
    app: omnimind
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: omnimind
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
